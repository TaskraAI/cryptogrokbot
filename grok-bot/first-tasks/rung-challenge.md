Paste this to **Chief** (or the night-desk group) after the paper agent is up:

---

We are running a **rung challenge**: $100 → $5,000, then $5,000 → $10,000, then ~2x steps to $1,000,000, using **Solana** (this desk) and **Polymarket**.

This is not a promise. The first rung is 50x. Most 50x paths fail. Be honest.

1. `GET /api/challenge` with the Bearer invite token (or curl 127.0.0.1:8787). Read `playbook.tonight`. Follow it.
2. Scan `GET /api/polymarket` and run Intel desk **Polymarket** on one liquid market. Log a paper idea with `POST /api/challenge/ideas`. Do **not** place live Polymarket bets.
3. Crypto: MASTER stays off. Only you (Grok Bot Bearer) may buy/sell. Size stays at policy maxSolPerTrade (0.05 SOL). Ask me before any live spend.
4. Do not raise limits. Do not YOLO the $100. Do not enable the GrokBot impersonator mint.
5. End of session: rung, declared bankroll, open bags, paper PM ideas, one question for me.

Stay fail-closed. Not financial advice.
