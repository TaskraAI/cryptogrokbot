# PAPER (default) never sends a transaction. LIVE requires MASTER_ENABLED=true.
MODE=PAPER
MASTER_ENABLED=false

# Dedicated hot-wallet secret. JSON byte array from Phantom/Solflare, or base58.
# Never commit a real key. Fund this wallet with only risk capital.
WALLET_SECRET_KEY=

# Solana RPC (Helius recommended). Fallback RPC used for exits if this fails.
HELIUS_RPC_URL=https://api.mainnet-beta.solana.com
FALLBACK_RPC_URL=https://api.mainnet-beta.solana.com

# Telegram cockpit
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: official X API v2 bearer for account timelines + search
X_BEARER_TOKEN=

# Optional: PumpPortal for pre-graduation bonding-curve local-sign trades
PUMPPORTAL_API_KEY=

# Grok / xAI (preferred). Enables /crew Grok agent + /research multi-agent.
XAI_API_KEY=
GROK_MODEL=grok-4-fast

# Optional OpenAI fallback if no XAI_API_KEY
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
LLM_TIMEOUT_MS=4000

# Live crew board (Scout, Sentinel, Grok, Scholar in parallel)
CREW_PORT=8787

# SQLite path
DATABASE_PATH=./data/night-agent.db

# Config dir
CONFIG_DIR=./config
